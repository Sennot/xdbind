{
  "release": "1.0.2",
  "passed": [
    "core_test: CML/CML uppercase extension and existing binding/load-scope checks, UBSan + warnings as errors",
    "catalog_test: CML; Folder replaces source; Auto uses configured root only; empty and missing selection stays empty; deleted files absent after refresh; nested Unicode paths; sidecars; duplicate paths; errors; cancellation; directory cycle",
    "static supported-DLL evidence: native load -> importer dispatcher -> CML header checker and decoder"
  ],
  "not_run": [
    "Windows/Geode build",
    "in-game CML loading/playback and UI"
  ],
  "changes": [
    "Add .cml extension",
    "Remove historical/autosave fallback search",
    "Saved bindings no longer inject files into current catalog",
    "Clear old rows as soon as a new scan starts",
    "Retain F8-only menu and previous CMake/manifest fixes"
  ]
}
